#include <iostream>
#include <math.h>
using namespace std;
 
int main()
{
    
    int a,b,c;
    float d,e,bmi;
    cout << "Enter your height: \n";
    cout << "Foots: ";
    cin>>a;
    cout << "\nInch: ";
    cin>>b;
    cout << "\nEnter your weight in foots: ";
    cin>>c;
 
    d=((a*12)+b)*0.0254;
   bmi=(c/2.2)/((((a*12)+b)*0.0254)*(((a*12)+b)*0.0254));
 
    cout <<"\nBMI = "<< bmi << "\n";
 
    system("Pause");
    return 0; 
}
