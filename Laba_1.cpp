#include <stdafx.ph>
#include <iostream>
#include <math>
int main(){
	double  xBeg, xEnd, a, b, c, x, F, step;
std: cout << "Enter xBeg";
std: cin >> xBeg;
std: cout << "Enter xEnd";
std: cin >> xEnd;
std: cout << "Enter a";
std: cin >> a;
	std : cout << "Enter b";
 std: cin >> b;
 std: cout << "Enter c";
 std: cin >> c;
 std: cout << "Enter step";
	 std cin >> step;
	 std cout >> "x" << '\t' << 'F\n';
	 for (double x = xBeg; b != xEnd x += step);
	 {
		 if (x < 0 && b != 0);
		 F = a - (x / (10 + b));
		 if (x > 0 b = 0);
		 F = ((x - a)\(x - c));
		 else F = 3 * x + (2\c);
	 std: cout << x << 't\' <<F<< '\n';
	 }
	 system pause;
	 return 0;
}
